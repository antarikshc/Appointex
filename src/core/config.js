/**
 * Returns the default config for bounds
 * All fields are in Minutes
 */
module.exports = {
  clockIn: 9 * 60,
  clockOut: 17 * 60,
  duration: 30,
  timeZoneOffset: 5.5 * 60, // India = +5:30
};
